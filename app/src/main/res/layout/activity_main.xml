<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <com.wangfangqi.widget.TitleView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        app:centerCustomView="@layout/layout_custom_right"
        app:centerType="text"
        app:isImmersion="true"
        app:leftCustomView="@layout/layout_custom_right"
        app:leftIcon="@drawable/wfq_ic_black_back"
        app:leftText="返回"
        app:leftTextColor="@android:color/white"
        app:leftType="textView"
        app:rightText="王方琪方式"
        app:rightIcon="@drawable/wfq_ic_black_back"
        app:rightTextColor="@android:color/white"
        app:rightType="textView"
        app:subTitle="subtitle-----"
        app:titleColor="@android:color/white"
        app:titleGravity="center" />

    <!--<View-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="0dp"-->
    <!--android:layout_weight="1"/>-->

</LinearLayout>